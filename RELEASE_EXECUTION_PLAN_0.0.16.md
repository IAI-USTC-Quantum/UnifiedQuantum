# UnifiedQuantum 0.0.16 — Pre-Release Test Execution Plan

Generated by `/uniqc-test-before-release`. Companion to `RELEASE_REPORT_0.0.16.md`.

This is the **second execution** of the plan. The first run (2026-07-18) validated the
release content as an uncommitted working tree and issued one gating condition: commit
the release content, then re-validate the tagged candidate. The maintainer has since
committed everything (`fea3d8b` feature + state dir, `5d3bf3a` changelog rotation,
`5c0d0b3` docs refresh, `b17988b` test fix). This run re-validates the **committed**
candidate per that condition.

## Release Candidate

- Repository: UnifiedQuantum (public, `IAI-USTC-Quantum/UnifiedQuantum`)
- Branch: `main` (aligned with `origin/main`, 0 ahead / 0 behind)
- HEAD commit: `b17988b` (`fix(test): normalize whitespace in backend-virtual CLI assertion`)
- Worktree: clean except untracked `.agents/` and this plan file — tested state = HEAD commit.
- Version (`uniqc/_version.py`, regenerated at install): `0.0.16.dev14`, `__commit_id__ = gd7001b7d4`
- Tag candidate: `v0.0.16` (not yet tagged; `git describe` → `v0.0.15-18-gb17988b`)
- Last released: `v0.0.15` (2026-06-04)
- Release scope (`CHANGELOG.md [0.0.16] - 2026-07-19`):
  1. **User-defined noisy virtual machines** (`dummy:virtual:<name>`) — `uniqc.backend_adapter.virtual_machine`, `uniqc.simulator.ThermalRelaxation`, CLI `uniqc backend virtual init|list|show|validate`, doc `docs/source/2_advanced/virtual_backends.md`.
  2. **Unified backend state directory** `~/.uniqc/backend/` (backend cache + chip cache, auto-migration from legacy paths).

## Environment Risk

Previous run's lead risk (release content uncommitted) is **resolved**: all 0.0.16
content is committed at HEAD `b17988b`; CHANGELOG rotated `[Unreleased]` → `[0.0.16] - 2026-07-19`.

Standing risk (B-2, pre-existing, not a 0.0.16 regression): `uv sync` fails on the
`[quark]` extra's `quarkcircuit` marker under uv's universal resolver (verified again
this run via `uv sync --extra all --group dev --group docs --dry-run`). Environment is
therefore the existing `.venv` built with `uv pip install -e ".[all]"`; commands run as
`.venv/bin/...` (as in the previous run). `commit 5c0d0b3` documented quark mirror
alternatives but did not change the resolver outcome.

## Credentials / Quotas Available

- Config: `~/.uniqc/config.yaml`, profile `default`.
- Tokens present: `originq`, `ibm` (rejected by IBM Quantum last run — external), `quafu` (**deprecated**).
- Token **missing**: `quark`. Quark discovery/execution BLOCKED.
- Real-device execution: **not authorized** in this session. Discovery + dry-run only;
  no real quantum task will be submitted. `--real-cloud-test` suite is collect-only.

## Commands, Expected Artifacts, Pass/Fail Criteria

### Phase 0 — Install / environment
- `.venv/bin/uniqc --version` → `uniqc 0.0.16.dev14`; `import uniqc` OK.
- `uv sync --extra all --group dev --group docs --dry-run` → reconfirm B-2 status.
- Pass: CLI entry works from `.venv`. Fail (blocker): maintained extra broken in `.venv`.

### Phase 1 — Automated Python tests
- `.venv/bin/python -m pytest uniqc/test -v --tb=short` (default; includes cloud discovery/status/token tests). Log: `/tmp/pytest_default.log`.
- `.venv/bin/python -m pytest uniqc/test --real-cloud-test --collect-only -q` (execution NOT authorized; collection check only).
- Pass: exit 0, no failures in maintained paths (qiskit/qutip/torch/docs/viz; quafu deprecated → not a blocker).
- Fail (blocker): any non-cloud, non-quafu failure.

### Phase 2 — C++ extension stubs
- `.venv/bin/python scripts/stubgen.py`; inspect `uniqc/simulator/uniqc_cpp.pyi` diff vs git.
- Pass: regenerates cleanly; diff empty or cosmetic.

### Phase 3 — Best-practices docs execution (release gate)
- `.venv/bin/python scripts/build_docs.py --only 3_best_practices`; `cd docs && make html` (venv on PATH).
- Inspect `docs/source/_generated/examples/3_best_practices/*.md` + built HTML.
- Pass: all 12 examples execute; HTML build succeeds; no stale commands.
- Fail (blocker): best-practices chapter fails to build or contradicts software.

### Phase 4 — CLI help + behavior
- Help: root, `python -m uniqc.cli`, and every public subcommand incl. `backend virtual {init,list,show,validate}` and `backend {list,show,update,chip-display}`.
- Behavior (Bell-state OriginIR): `circuit --info`; `simulate`; `submit --backend dummy:local:simulator --wait`; `submit --backend dummy:local:virtual-line-3 --wait`; `submit --backend dummy:originq:WK_C180 --dry-run`; `backend update --platform originq`; `backend list --platform originq`; `backend show originq:WK_C180`; `backend chip-display originq/WK_C180 --update`.
- Headline feature: `backend virtual init <name>` → `validate` → `submit --backend dummy:virtual:<name> --wait` (noisy result expected); `virtual:<name>` enumerable in `backend list`; `dummy:<platform>:<backend>` not enumerable.
- AI hints: `config list --ai-hint`; `config always-ai-hint on` → bare command prints hints → `config always-ai-hint off`.
- Pass: help matches docs; workflows execute; dummy semantics correct.

### Phase 5 — Gateway frontend + API
- `cd frontend && npm ci && npm run build`.
- `.venv/bin/uniqc gateway start --host 127.0.0.1 --port 18765`; `gateway status`.
- HTTP: `/api/health`, `/api/version`, `/api/backends`, `/api/tasks`, `/` (React app serves `frontend/dist`).
- Backend cards exclude `dummy:<platform>:<backend>`; include `virtual:<name>`.
- Pass: build succeeds; API healthy; schema matches frontend expectations. Visual gap if no browser tool.

### Phase 6 — Real-platform validation
- Discovery: `backend update --platform originq` (live), `backend list --platform originq`, `backend show originq:WK_C180`. IBM: expect BLOCKED (invalid token, external). Quark: BLOCKED (no token).
- Dry-run: `submit <bell> --backend dummy:originq:WK_C180 --dry-run` (naive 2q Bell → correct topology rejection; single-qubit dry-run passes).
- Execution: NOT RUN (no quota authorization this session).
- Pass: ≥1 OriginQ live discovery + dry-run; each unavailable platform has a recorded external cause.

### Phase 7 — Programmatic docs alignment
- Scripted: `pyproject.toml` scripts vs docs; CLI docs vs captured help; removed top-level imports (`uniqc.transpiler/task/qasm/originir/pytorch/analyzer`) not referenced; dummy semantics; config path `~/.uniqc/config.yaml`; AI-hint surface; IBM nested proxy key; Quafu deprecation.
- Pass: no doc recommends a removed command/API; semantics consistent.

## Recommendation Rules (from skill)

`DO NOT RELEASE` if any maintained best-practices/CLI/gateway/test/docs path fails or
docs claim a workflow that does not work. `RELEASE WITH KNOWN GAPS` only for external,
documented, off-recommended-path gaps. `RELEASE` only when mandatory phases pass and
untested areas are non-critical with maintainer approval.
